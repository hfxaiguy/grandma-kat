# Hugging Face API token (read access is sufficient for the router)
# Get one at https://huggingface.co/settings/tokens
HF_TOKEN=your-token-here

# Optional: override the CDP port (defaults to 9222)
# CDP_PORT=9222